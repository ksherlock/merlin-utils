dist: bionic
language: generic
os: osx
matrix:
  include:
    - os: linux
      env: COMPILER_NAME=gcc CXX=g++ CC=gcc
    - os: linux
      env: COMPILER_NAME=clang CXX=clang++-3.8 CC=clang-3.8

script: make all

compiler:
  - clang
  - gcc

